# Robolectric 4.15 ships images up to API 35. The app targets 36, so pin the tests to the newest
# API Robolectric can actually emulate; minSdk is 35, so this is still a supported configuration.
sdk=35
