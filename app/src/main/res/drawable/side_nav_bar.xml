<!-- Drawer header. Indigo, matching the app bar rather than the template's teal. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="135"
        android:startColor="@color/ink_raised"
        android:centerColor="@color/ink"
        android:endColor="@color/ink_deep"
        android:type="linear" />
</shape>
