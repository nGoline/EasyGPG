*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# IDE — machine-specific, not for version control
/.idea/

# Signing material — never commit these
*.jks
*.keystore
*.pem
keystore.properties
signing.properties

# Release runbook — describes the signing and Play setup, kept out of the public repo
/RELEASING.md

# Build outputs / release artifacts
*.apk
*.aab
/app/release/
